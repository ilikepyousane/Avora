<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Avora.Views.Settings.DebugConsole"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Disabled"
    AutomationProperties.Name="Отладочная консоль">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Margin="0,0,0,10">
            <Button Click="BackButton_Click" AutomationProperties.Name="Назад">
                <FontIcon Glyph="&#xE72B;"/>
            </Button>
            <TextBlock Text="Отладочная консоль" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10" Margin="0,0,0,10">
            <CheckBox x:Name="AutoScrollCheckBox" Content="Автопрокрутка" IsChecked="True" VerticalAlignment="Center"/>
            <Button Content="Копировать" Click="CopyButton_Click" AutomationProperties.Name="Копировать лог"/>
            <Button Content="Очистить" Click="ClearButton_Click" AutomationProperties.Name="Очистить лог"/>
        </StackPanel>

        <Border Grid.Row="2" Background="#1E1E1E" CornerRadius="8" Padding="5">
            <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="LogTextBlock"
                           FontFamily="Consolas"
                           FontSize="12"
                           TextWrapping="Wrap"
                           Foreground="#D4D4D4"
                           AutomationProperties.Name="Лог отладки"/>
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="3" x:Name="StatusText" Text="Строк: 0" FontSize="11" Opacity="0.5" Margin="0,5,0,0"/>
    </Grid>
</Page>
